<%= form_for(entry_page) do |f| %>
  <% if entry_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry_page.errors.count, "error") %> prohibited this entry_page from being saved:</h2>

      <ul>
      <% entry_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :published %>
    <%= f.check_box :published %>
  </div>

  <div class="field">
    <%= f.label :section_id %>
    <%= f.text_field :section_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
